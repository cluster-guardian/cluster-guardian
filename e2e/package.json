{
  "name": "cluster-guardian-e2e",
  "private": true,
  "description": "UI end-to-end tests: boot `serve --fixture` and drive the dashboard with Playwright.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
